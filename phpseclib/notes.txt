Common\Functions\Arrays:

  - ArrayAccessException (extends UnexpectedValueException) -- should we just make this UnexpectedValueException?

Common\Functions\Strings:

  - UnexpectedValueException
  - UnsupportedValueException
  - ConflictingStateException (extends InvalidStateException)

Crypt\Common\AsymmetricKey:

  - InvalidStateException
  - ExceptionInterface
  - NoKeyLoadedException (extends UnexpectedValueException)
  - UnsupportedFormatException (extends UnsupportedValueException)
  - BadConfigurationException

Crypt\Common\SymmetricKey:

  - BadModeException (extends UnsupportedValueException)
  - BadMethodCallException (extends InvalidStateException)
  - LengthException (extends UnsupportedValueException)
  - ConflictingStateException (extends InvalidStateException)
  - MissingArgumentsException (extends InvalidStateException)
  - UnsupportedAlgorithmException (extends UnsupportedValueException)
  - BadDecryptionException (extends UnexpectedValueException)
  - InvalidStateException

Crypt\Common\Formats\JWK:

  - UnsupportedValueException
  - UnexpectedValueException

Crypt\Common\Formats\Keys\OpenSSH:

  - UnexpectedValueException
  - UnsupportedValueException
  - UnsupportedAlgorithmException (extends UnsupportedValueException)
  - PasswordNeededException (extends InvalidStateException)

Crypt\Common\Formats\Keys\PKCS8:

  - PasswordNeededException (extends InvalidStateException)
  - UnexpectedValueException
  - UnsupportedValueException
  - UnsupportedAlgorithmException (extends UnsupporteedValueException)

Crypt\Common\Formats\Keys\PKCS1:

  - PasswordNeededException (extends InvalidStateException)
  - UnexpectedValueException
  - UnsupportedAlgorithmException (extends UnsupportedValueException)
  - BadModeException (extends UnsupportedModeException)

Crypt\Common\Formats\Keys\PuTTY:

  - UnsupportedValueException
  - BadConfigurationException
  - UnexpectedValueException
  - UnsupportedAlgorithmException (extends UnsupportedValueException)

Crypt\Common\BlockCipher

  - BadModeException (extends UnsupportedValueException)

Crypt\AES:

  - BadMethodCallException (extends InvalidStateException)
  - LengthException (extends UnsupportedValueException)

Crypt\Blowfish:

  - LengthException (extends UnsupportedValueException)

Crypt\ChaCha20:

  - InvalidStateException
  - BadDecryptionException (extends UnexpectedValueException)

Crypt\DES:

  - LengthException (extends UnsupportedValueException)

Crypt\RC2:

  - LengthException (extends UnsupportedValueException)

Crypt\Rijndael:

  - LengthException (extends UnsupportedValueException)


File\ASN1.php

  - UnexpectedValueException
  - EOCException (extends UnsupportedValueException) (this one is specifically caught in places)
  - EncodedDataUnavailableException (extends UnsupportedValueException) (this one is specifically caught in places)
  - UnsupportedValueException
  - ResourceLimitException (extends UnsupportedValueException)




questions:
in Arrays.php do we replace ArrayAccessException with UnexpectedValueSyntax?
in SymmetricKey.php there's ConflictingStateException...  maybe that should just be changed to InvalidStateException since it's pretty much used nowhere else?
maybe rename ExceptionInterface to PhpseclibException?
in Crypt\Common\SymmetricKey.php there's MissingArgumentsException... maybe that should just be changed to InvalidArgumentsException (which is used in DH.php)?
in Crypt\Common\AsymmetricKey.php there's UnsupportedFormatException...  maybe we should replace that with UnsupportedValueException?





i think Rector will be able to take care of most exceptions save for FileSystemException. we'll prob need to create that one ourselves.






replace sftp_get_packet with this:

$response = $this->get_sftp_packet_or_error([SFTPPacketType::VERSION]);

make unexpectedvalueexception look more like this:

                    if ($status != StatusCode::OK) {
                        throw new UnexpectedValueException(
                            'Expected SSH_FX_OK. Got ' . StatusCode::getConstantNameByValue($status)
                        );
                    }

                    if ($status != StatusCode::EOF) {
                        throw $this->throwStatusError($response, $status);
                    }

a lot of times it's stuff like this (from realpath but also present in chdir, readlist, stat_helper, touch, readlink, put):

            $response = $this->get_sftp_packet_or_error([SFTPPacketType::NAME, SFTPPacketType::STATUS]);
            switch ($this->packet_type) {
                case SFTPPacketType::NAME:
                    // do stuff
                case SFTPPacketType::STATUS:
                    throw $this->throwStatusError($response);
            }

i feel like the SFTPPacketType::STATUS bit is present quite frequently. maybe we could update get_sftp_packet_or_error() to handle that case as well?

although sometimes we want it to only throw a status error when the status code isn't EOF. eg. (from readlist):

            switch ($this->packet_type) {
                case SFTPPacketType::NAME:
                    // do stuff
                case SFTPPacketType::STATUS:
                    [$status] = Strings::unpackSSH2('N', $response);
                    if ($status != StatusCode::EOF) {
                        throw $this->throwStatusError($response, $status);
                    }
                    break 2;
            }

setstat, symlink, mkdir_helper, close_handle is similar to the above:

        $response = $this->get_sftp_packet_or_error([SFTPPacketType::STATUS]);
        [$status] = Strings::unpackSSH2('N', $response);
        if ($status != StatusCode::OK) {
            throw $this->throwStatusError($response, $status);
        }

read_put_responses is different:

            $response = $this->get_sftp_packet_or_error([SFTPPacketType::STATUS]);
            [$status] = Strings::unpackSSH2('N', $response);
            if ($status != StatusCode::OK) {
                if (!isset($files)) {
                    throw $this->throwStatusError($response, $status);
                }
                $this->logError($response, $status, $files[$size - $i]);
            }

delete is another beast as well:

        [$status] = Strings::unpackSSH2('N', $response);
        if ($status != StatusCode::OK) {
            if ($recursive) {
                // if you're trying to delete a directory you'll have 1x entry in getSFTPErrors() as it'll
                // first try to delete it as a file. if the file you're deleting doesn't exist you'll have
                // 2x entries - the first one being for your attempt to delete the "file" as a directory
                // and the second attempting to delete the "file" as a file
                $this->logError($response, $status, "REMOVE $path");
            } else {
                throw $this->throwStatusError($response, $status);
            }

            $files = [];
            $this->delete_recursive($path, $files);
            $this->read_put_responses($files);
            return;
        }

an easy gotcha to look out for:

get_sftp_packet([

do ctrl + f to make sure that doesn't occur anyhwere










likewise, in SSH2 there's this:

    private function get_binary_packet_or_close(int ...$message_types): string
    {
        try {
            $packet = $this->get_binary_packet();
            if (count($message_types) > 0 && !in_array(ord($packet[0]), $message_types)) {
                $this->disconnect_helper(DisconnectReason::PROTOCOL_ERROR);
                throw new ConnectionClosedException('Bad message type. Expected: #'
                    . implode(', #', $message_types) . '. Got: #' . ord($packet[0]));
            }
            return $packet;
        } catch (TimeoutException) {
            $this->disconnect_helper(DisconnectReason::BY_APPLICATION);
            throw new ConnectionClosedException('Connection closed due to timeout');
        }
    }

looks like it's throwing numbers - not more useful strings
